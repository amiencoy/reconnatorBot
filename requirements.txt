# ==================================================================================== #
# This is the requirements.txt file. The grocery list for our Python environment.      #
# It contains all the essential async libraries (aiogram, httpx, aiohttp) and the      #
# Model Context Protocol (mcp, fastmcp) packages that give the agent its brain.        #
# ==================================================================================== #

# Direct runtime dependencies are pinned so container builds are reproducible.
# Update these pins through Dependabot and verify them on Python 3.11 and 3.14.
aiogram==3.31.0
fastmcp==4.0.2
httpx==0.28.1
mcp==1.29.1
msgpack==1.2.2
python-dotenv==1.2.3
PyYAML==6.0.3
reportlab==5.0.1
setuptools==84.0.0
